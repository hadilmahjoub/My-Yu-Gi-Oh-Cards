<form method="post" action="{{ path('app_showcase_delete', {'id': showcase.id}) }}" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showcase.id) }}">
    <button class="btn btn-danger mt-2 mb-2">Delete</button>
</form>
