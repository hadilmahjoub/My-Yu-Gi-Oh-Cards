<form method="post" action="{{ path('app_ygo_card_delete', {'id': ygo_card.id}) }}" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ygo_card.id) }}">
    <button class="btn btn-danger mt-2 mb-2">Delete</button>
</form>
