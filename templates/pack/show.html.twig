{% extends 'base.html.twig' %}

{% block title %}
	Pack n°{{ pack.id }}
{% endblock %}

{% block main %}
    <h1>Pack Details</h1>

    {% dump pack %}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>YGO Cards</th>
                <th>&nbsp;</th>
                
            </tr>
           </thead>
           
        <tbody>     
			<tr>
                <td>{{ pack.id }}</td>                
                <td>{{ pack.title }}</td>

                <td>
                  <ul>
                  {%  for ygoCard in pack.getYgoCards() %}
                      <li>
                      {{ ygoCard.id }} {{ ygoCard.name }}
                      </li>
                  {% endfor %} {# pack.ygo_cards #}
                  </ul> 
                </td>
              
                <td>
                  <ul>
                  {%  for ygoCard in pack.getYgoCards() %}
                      <li>
                			<a href="{{ path('app_ygo_card_show', {'id': ygoCard.id}) }}" 
                			   class="btn btn-secondary mt-2 mb-2">
                			   		Show details for {{ ygoCard.name }}
                			</a>
                	  </li>
                  {% endfor %} {# pack.ygo_cards #}
                  </ul> 
                </td>
			</tr>
        </tbody>
    </table>

   <a href="{{ path('app_user_show', {'id': pack.user.id}) }}" class="btn btn-primary mt-2 mb-2">Back to User</a>
   <a href="{{ path('app_ygo_card_new', {'id': pack.id}) }}" class="btn btn-success mt-2 mb-2">Add new YGOCard</a>
   


{% endblock %} {# main #} 

{# ... #} 
